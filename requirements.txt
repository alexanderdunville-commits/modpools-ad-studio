anthropic>=0.69
openai>=1.40
httpx>=0.27
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
SQLAlchemy>=2.0
cryptography>=42.0
# Postgres driver — used in production when DATABASE_URL points at Postgres
# (persistent storage). Harmless when running on the local SQLite default.
psycopg[binary]>=3.1
